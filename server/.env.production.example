# ===========================================
# LineClock Production Environment Variables
# ===========================================
# 
# IMPORTANT: 
# 1. Copy this file to .env.production
# 2. Fill in all values with REAL production credentials
# 3. NEVER commit .env.production to version control
# 4. Keep this file secure - it contains sensitive data

# Server Configuration
PORT=3001
NODE_ENV=production

# Database (PostgreSQL)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
# Example: postgresql://lineclock_user:strongpassword@db.railway.app:5432/lineclock_prod
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE"

# JWT Secret (CRITICAL - Generate a strong random string)
# Run this command to generate: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=REPLACE_WITH_STRONG_RANDOM_64_CHAR_STRING

# Microsoft Entra ID (Azure AD) Configuration
ENTRA_CLIENT_ID=your-azure-app-client-id
ENTRA_TENANT_ID=your-azure-tenant-id
ENTRA_CLIENT_SECRET=your-azure-app-client-secret
ENTRA_REDIRECT_URI=https://lineclock.smartfactory.io/auth/callback

# CORS (Frontend URL)
CORS_ORIGIN=https://lineclock.smartfactory.io

# Bill.com Integration (Optional - can configure in UI)
# Leave blank if you want to configure via the admin dashboard
BILLCOM_DEV_KEY=
BILLCOM_USERNAME=
BILLCOM_PASSWORD=
BILLCOM_ORG_ID=
BILLCOM_ENVIRONMENT=PRODUCTION

# Azure Communication Services (Optional - for notifications)
# Leave blank to disable email/SMS notifications
AZURE_COMMUNICATION_CONNECTION_STRING=
AZURE_COMMUNICATION_EMAIL_SENDER=

# ===========================================
# Deployment Checklist:
# ===========================================
# [ ] Set DATABASE_URL to PostgreSQL connection string
# [ ] Generate and set strong JWT_SECRET (64+ chars)
# [ ] Update ENTRA_REDIRECT_URI to production domain
# [ ] Update CORS_ORIGIN to production domain
# [ ] Update Entra ID app registration redirect URIs
# [ ] Set NODE_ENV=production
# [ ] Test health endpoint after deployment
# [ ] Run database migrations: npx prisma migrate deploy
# [ ] Create first admin user by logging in with Entra ID
