# Server Configuration
PORT=3001
HOST=0.0.0.0
NODE_ENV=production

# Database - PostgreSQL
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://username:password@localhost:5432/lineclock"

# JWT Secret (generate: openssl rand -hex 32)
JWT_SECRET=your-secure-jwt-secret-here

# Entra ID (Azure AD) Configuration
ENTRA_CLIENT_ID=your-entra-client-id
ENTRA_TENANT_ID=your-entra-tenant-id
ENTRA_CLIENT_SECRET=your-entra-client-secret
ENTRA_REDIRECT_URI=https://yourdomain.com/auth/callback

# CORS - Frontend URL
CORS_ORIGIN=https://yourdomain.com

# First Admin(s) - comma-separated emails that auto-promote to ADMIN role
FIRST_ADMIN_EMAILS=admin@yourdomain.com

# Encryption key for sensitive data (Bill.com credentials)
# Generate: openssl rand -hex 32
ENCRYPTION_KEY=your-encryption-key-here

# Azure Communication Services (optional)
AZURE_COMMUNICATION_EMAIL_CONNECTION_STRING=
AZURE_COMMUNICATION_SMS_CONNECTION_STRING=
SENDER_EMAIL=noreply@yourdomain.com
SENDER_PHONE=
